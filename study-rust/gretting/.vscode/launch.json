{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "Debug",
          "type": "cppvsdbg",
          "preLaunchTask": "build",
          "request": "launch",
          // "target": "${workspaceFolder}/target/debug/${workspaceFolderBasename}",
          "program": "${workspaceRoot}/src/order_arr.exe",
          "args": [],
          "cwd": "${workspaceFolder}",
          "stopAtEntry": false,
          "environment": [],
          "console": "integratedTerminal",
      }
  ]
}